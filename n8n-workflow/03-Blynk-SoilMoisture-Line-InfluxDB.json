{
  "name": "03-Blynk-SoilMoisture-Line-InfluxDB",
  "nodes": [
    {
      "parameters": {},
      "id": "a35dc917-6b01-49da-9b69-02a14c5350ad",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        -1376,
        64
      ]
    },
    {
      "parameters": {
        "triggerTimes": {
          "item": [
            {
              "mode": "everyMinute"
            }
          ]
        }
      },
      "id": "8e776b1c-b8d2-4a52-a5ba-b66b7ecd8b71",
      "name": "Cron (ทุก 1 นาที)",
      "type": "n8n-nodes-base.cron",
      "typeVersion": 1,
      "position": [
        -1376,
        272
      ]
    },
    {
      "parameters": {
        "text": "={{ $json.message }}"
      },
      "type": "n8n-nodes-linewebhook.LineMessageNode",
      "typeVersion": 1,
      "position": [
        224,
        0
      ],
      "id": "78942c0b-61b8-4001-ad81-984b3d42715a",
      "name": "LineMessageNode"
    },
    {
      "parameters": {
        "message": "={{ $json.message }}",
        "replyToken": "=",
        "targetRecipient": "={{ $('Set (LINE Target)').item.json.toId }}"
      },
      "type": "n8n-nodes-linewebhook.LineMessaging",
      "typeVersion": 1,
      "position": [
        448,
        0
      ],
      "id": "13e424ff-82ba-43a2-a970-38191ea034ae",
      "name": "LineMessaging-SendMessage",
      "credentials": {
        "lineMessagingAuthApi": {
          "id": "8DphaWow38ENbUaZ",
          "name": "Line Messaging Auth-xn8nsmartfarm"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const BLYNK_BASE = \"http://IP-Address:Port\";\nconst BLYNK_TOKEN = \"Replace-Your-Blynk-Token-Here\";\n\nconst pins = [\n  {\n    key: \"valve1\",\n    virtual_pin: \"V1\",\n    label: \"Valve 1\",\n    unit: \"\"\n  },\n  {\n    key: \"soil\",\n    virtual_pin: \"V2\",\n    label: \"Soil Moisture\",\n    unit: \"%\"\n  },\n  {\n    key: \"auto_mode\",\n    virtual_pin: \"V3\",\n    label: \"Auto Manual Mode\",\n    unit: \"\"\n  },\n  {\n    key: \"slider_threshold\",\n    virtual_pin: \"V4\",\n    label: \"Soil Moisture Slider Threshold\",\n    unit: \"%\"\n  }\n];\n\nreturn pins.map(pin => {\n  return {\n    json: {\n      ...pin,\n      url: `${BLYNK_BASE}/${BLYNK_TOKEN}/get/${pin.virtual_pin}`\n    }\n  };\n});"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -1056,
        160
      ],
      "id": "cdef8b02-e48c-403e-946c-5955c23115fd",
      "name": "Prepare Blynk Pin List"
    },
    {
      "parameters": {
        "url": "={{ $json.url }}",
        "options": {
          "response": {}
        }
      },
      "id": "ce7b1f6c-6466-4105-b2d0-aebb44f4f073",
      "name": "HTTP get Blynk Values",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -816,
        160
      ]
    },
    {
      "parameters": {
        "jsCode": "function extractBlynkValue(input) {\n  let rawValue;\n\n  if (typeof input === \"string\") {\n    rawValue = input;\n  } \n  else if (typeof input === \"number\") {\n    rawValue = input;\n  }\n  else if (Array.isArray(input)) {\n    rawValue = input[0];\n  } \n  else if (typeof input.body === \"string\") {\n    rawValue = input.body;\n  } \n  else if (Array.isArray(input.body)) {\n    rawValue = input.body[0];\n  } \n  else if (typeof input.data === \"string\") {\n    rawValue = input.data;\n  } \n  else if (Array.isArray(input.data)) {\n    rawValue = input.data[0];\n  }\n  else {\n    const values = Object.values(input);\n\n    if (values.every(v => typeof v === \"string\" && v.length === 1)) {\n      rawValue = values.join(\"\");\n    } else {\n      rawValue = values[0];\n    }\n  }\n\n  return rawValue;\n}\n\nfunction toBool(value) {\n  return [\"1\", \"true\", \"on\", \"auto\", \"AUTO\"].includes(String(value).trim());\n}\n\n// ===============================\n// 1) ดึงข้อมูลจาก Blynk V1-V4\n// ===============================\nconst pinItems = $(\"Prepare Blynk Pin List\").all();\nconst responseItems = $input.all();\n\nconst raw = {};\n\nfor (let i = 0; i < responseItems.length; i++) {\n  const key = pinItems[i].json.key;\n  const response = responseItems[i].json;\n  raw[key] = extractBlynkValue(response);\n}\n\n// ===============================\n// 2) แปลงค่าข้อมูล\n// ===============================\nconst valve1 = toBool(raw.valve1);\nconst valve1Status = valve1 ? \"ON\" : \"OFF\";\n\nconst autoMode = toBool(raw.auto_mode);\nconst autoModeText = autoMode ? \"Auto\" : \"Manual\";\n\nconst soilRaw = Number(raw.soil);\nconst thresholdRaw = Number(raw.slider_threshold);\n\nconst soilMoisture = Number.isFinite(soilRaw)\n  ? Number(soilRaw.toFixed(1))\n  : null;\n\nconst sliderThreshold = Number.isFinite(thresholdRaw)\n  ? Number(thresholdRaw.toFixed(1))\n  : null;\n\n// ===============================\n// 3) ตั้งค่า Alert Policy\n// ===============================\nconst cooldownMinutes = 15;   // เปลี่ยนเป็น 30 ได้ถ้าต้องการแจ้งซ้ำทุก 30 นาที\nconst hysteresis = 1.0;       // ค่ากลับสู่ปกติต้อง >= Slider + 1.0 เพื่อกันแจ้งเตือนสลับไปมา\n\nconst now = new Date();\nconst nowMs = Date.now();\n\nconst thaiTime = now.toLocaleString(\"th-TH\", {\n  timeZone: \"Asia/Bangkok\",\n  year: \"numeric\",\n  month: \"2-digit\",\n  day: \"2-digit\",\n  hour: \"2-digit\",\n  minute: \"2-digit\",\n  second: \"2-digit\"\n});\n\nconst siteId = \"site01\";\nconst deviceId = \"esp32_smartfarm_01\";\nconst slaveId = 1;\n\n// ใช้จำสถานะข้ามรอบการทำงานของ Workflow\nconst staticData = $getWorkflowStaticData(\"global\");\nconst alertKey = `${siteId}_${deviceId}_soil_slave_${slaveId}`;\n\nconst lastAlertAt = staticData[`lastAlertAt_${alertKey}`] || 0;\nconst wasInAlert = staticData[`wasInAlert_${alertKey}`] || false;\n\n// ===============================\n// 4) ตรวจสอบเงื่อนไข\n// ===============================\nconst isLowSoil =\n  autoMode === true &&\n  soilMoisture !== null &&\n  sliderThreshold !== null &&\n  soilMoisture < sliderThreshold;\n\nconst isRecovered =\n  autoMode === true &&\n  wasInAlert === true &&\n  soilMoisture !== null &&\n  sliderThreshold !== null &&\n  soilMoisture >= sliderThreshold + hysteresis;\n\nlet shouldSendLine = false;\nlet eventStatus = \"normal\";\nlet lineMessage = \"\";\n\n// ===============================\n// 5) กรณีความชื้นต่ำ\n// ===============================\nif (isLowSoil) {\n  const cooldownPassed =\n    !lastAlertAt || (nowMs - lastAlertAt) >= cooldownMinutes * 60 * 1000;\n\n  if (!wasInAlert) {\n    // แจ้งเตือนครั้งแรกทันที\n    shouldSendLine = true;\n    eventStatus = \"low_soil_moisture_first_alert\";\n\n    staticData[`lastAlertAt_${alertKey}`] = nowMs;\n    staticData[`wasInAlert_${alertKey}`] = true;\n  } \n  else if (cooldownPassed) {\n    // แจ้งซ้ำเมื่อยังต่ำอยู่ และครบเวลา Cooldown แล้ว\n    shouldSendLine = true;\n    eventStatus = \"low_soil_moisture_repeat_alert\";\n\n    staticData[`lastAlertAt_${alertKey}`] = nowMs;\n    staticData[`wasInAlert_${alertKey}`] = true;\n  } \n  else {\n    // ยังต่ำอยู่ แต่ยังไม่ครบเวลาแจ้งซ้ำ\n    shouldSendLine = false;\n    eventStatus = \"low_soil_moisture_waiting_cooldown\";\n\n    staticData[`wasInAlert_${alertKey}`] = true;\n  }\n\n  if (shouldSendLine) {\n    lineMessage =\n`⚠️ แจ้งเตือนความชื้นในดินต่ำ\n\nพื้นที่: ${siteId}\nอุปกรณ์: ${deviceId}\nเซนเซอร์: Soil Moisture Slave ID ${slaveId}\n\nค่าความชื้นปัจจุบัน: ${soilMoisture.toFixed(1)} %\nค่าที่ตั้งไว้บน Slider: ${sliderThreshold.toFixed(1)} %\n\nสถานะโหมด: ${autoModeText}\nสถานะวาล์ว 1: ${valve1Status}\n\nคำแนะนำ: ค่าความชื้นต่ำกว่าที่กำหนดไว้ ควรตรวจสอบการทำงานของวาล์วหรือระบบให้น้ำ\n\nเวลา: ${thaiTime}`;\n  }\n}\n\n// ===============================\n// 6) กรณีค่ากลับมาปกติ\n// ===============================\nelse if (isRecovered) {\n  shouldSendLine = true;\n  eventStatus = \"recovered\";\n\n  staticData[`wasInAlert_${alertKey}`] = false;\n  staticData[`lastAlertAt_${alertKey}`] = 0;\n\n  lineMessage =\n`✅ ความชื้นในดินกลับสู่ระดับปกติ\n\nพื้นที่: ${siteId}\nอุปกรณ์: ${deviceId}\nเซนเซอร์: Soil Moisture Slave ID ${slaveId}\n\nค่าความชื้นปัจจุบัน: ${soilMoisture.toFixed(1)} %\nค่าที่ตั้งไว้บน Slider: ${sliderThreshold.toFixed(1)} %\n\nสถานะโหมด: ${autoModeText}\nสถานะวาล์ว 1: ${valve1Status}\n\nสถานะ: ค่าความชื้นกลับเข้าสู่ระดับปกติแล้ว\n\nเวลา: ${thaiTime}`;\n}\n\n// ===============================\n// 7) กรณีปกติ\n// ===============================\nelse {\n  shouldSendLine = false;\n\n  if (!autoMode) {\n    eventStatus = \"manual_mode\";\n  } else {\n    eventStatus = \"normal\";\n  }\n\n  // ถ้า Auto Mode เปิดอยู่ และค่าอยู่ในเกณฑ์ปกติ ให้ reset alert state\n  if (autoMode && soilMoisture !== null && sliderThreshold !== null && soilMoisture >= sliderThreshold + hysteresis) {\n    staticData[`wasInAlert_${alertKey}`] = false;\n    staticData[`lastAlertAt_${alertKey}`] = 0;\n  }\n}\n\n// ===============================\n// 8) ส่งออกข้อมูล\n// ===============================\nreturn [\n  {\n    json: {\n      site_id: siteId,\n      device_id: deviceId,\n\n      sensor_type: \"soil_moisture\",\n      sensor_name: \"soil_moisture_slave_1\",\n      modbus_slave_id: slaveId,\n      slave_id: slaveId,\n\n      valve1: valve1,\n      valve1_raw: raw.valve1,\n      valve1_status: valve1Status,\n      valve1_virtual_pin: \"V1\",\n\n      soil_moisture: soilMoisture,\n      soil_moisture_display: soilMoisture !== null ? soilMoisture.toFixed(1) + \" %\" : null,\n      soil_virtual_pin: \"V2\",\n\n      auto_mode: autoMode,\n      auto_mode_text: autoModeText,\n      auto_mode_raw: raw.auto_mode,\n      auto_mode_virtual_pin: \"V3\",\n\n      slider_threshold: sliderThreshold,\n      slider_threshold_display: sliderThreshold !== null ? sliderThreshold.toFixed(1) + \" %\" : null,\n      slider_virtual_pin: \"V4\",\n\n      unit: \"%\",\n\n      should_send_line: shouldSendLine,\n      event_status: eventStatus,\n      cooldown_minutes: cooldownMinutes,\n      hysteresis: hysteresis,\n\n      line_message: lineMessage,\n\n      timestamp: now.toISOString(),\n      timestamp_th: thaiTime,\n\n      raw_blynk_values: raw\n    }\n  }\n];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -544,
        160
      ],
      "id": "522e52aa-3b3f-4b86-af70-fa903739db86",
      "name": "Format Blynk All Values JSON"
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "baf6e00d-f534-4213-aeec-e7a2bd96db7f",
              "leftValue": "={{ $json.should_send_line }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        -320,
        160
      ],
      "id": "bd7c155f-6b09-407f-9708-f3286ef88c22",
      "name": "IF: should_send_line"
    },
    {
      "parameters": {
        "values": {
          "string": [
            {
              "name": "toId",
              "value": "=Your user ID"
            },
            {
              "name": "message",
              "value": "={{ $json.line_message }}"
            }
          ]
        },
        "options": {}
      },
      "name": "Set (LINE Target)",
      "type": "n8n-nodes-base.set",
      "typeVersion": 2,
      "position": [
        0,
        0
      ],
      "id": "3e6b7b07-e01e-4eb0-86ca-e1a5f818169e",
      "alwaysOutputData": false
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://InfluxDB-IP-Address:8086/api/v2/write?org=smfthailand&bucket=smf001&precision=s",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "Token <INFLUXDB_TOKEN>"
            },
            {
              "name": "Content-Type",
              "value": "text/plain; charset=utf-8"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "text/plain",
        "body": "={{ $json.influx_line }}",
        "options": {}
      },
      "id": "1ba5b239-b56c-499d-9926-4cd7ebb5e144",
      "name": "HTTP Request",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        80,
        384
      ]
    },
    {
      "parameters": {
        "jsCode": "const data = $json;\n\nfunction escapeTag(value) {\n  return String(value ?? \"\")\n    .replace(/ /g, \"\\\\ \")\n    .replace(/,/g, \"\\\\,\")\n    .replace(/=/g, \"\\\\=\");\n}\n\nfunction toNumber(value, fallback = 0) {\n  const n = Number(value);\n  return Number.isFinite(n) ? n : fallback;\n}\n\nconst measurement = \"smartfarm_soil\";\n\nconst siteId = escapeTag(data.site_id || \"site01\");\nconst deviceId = escapeTag(data.device_id || \"esp32_smartfarm_01\");\nconst sensorName = escapeTag(data.sensor_name || \"soil_moisture_slave_1\");\nconst slaveId = escapeTag(data.slave_id || 1);\n\nconst soilMoisture = toNumber(data.soil_moisture);\nconst sliderThreshold = toNumber(data.slider_threshold);\n\nconst valve1Value = data.valve1 === true ? 1 : 0;\nconst autoModeValue = data.auto_mode === true ? 1 : 0;\nconst shouldSendLineValue = data.should_send_line === true ? 1 : 0;\n\n// ใช้ timestamp เป็นวินาที เพราะ HTTP Request จะกำหนด precision=s\nconst timestamp = Math.floor(Date.now() / 1000);\n\nconst lineProtocol =\n`${measurement},site_id=${siteId},device_id=${deviceId},sensor_name=${sensorName},slave_id=${slaveId} ` +\n`soil_moisture=${soilMoisture},slider_threshold=${sliderThreshold},valve1=${valve1Value},auto_mode=${autoModeValue},should_send_line=${shouldSendLineValue} ${timestamp}`;\n\nreturn [\n  {\n    json: {\n      ...data,\n      influx_line: lineProtocol\n    }\n  }\n];"
      },
      "id": "71c302c9-b26a-4cfc-88a3-87c0a6fe5436",
      "name": "To InfluxDB Line Protocol",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -304,
        384
      ]
    }
  ],
  "pinData": {},
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Prepare Blynk Pin List",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cron (ทุก 1 นาที)": {
      "main": [
        [
          {
            "node": "Prepare Blynk Pin List",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "LineMessageNode": {
      "main": [
        [
          {
            "node": "LineMessaging-SendMessage",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Blynk Pin List": {
      "main": [
        [
          {
            "node": "HTTP get Blynk Values",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "HTTP get Blynk Values": {
      "main": [
        [
          {
            "node": "Format Blynk All Values JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Blynk All Values JSON": {
      "main": [
        [
          {
            "node": "IF: should_send_line",
            "type": "main",
            "index": 0
          },
          {
            "node": "To InfluxDB Line Protocol",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Set (LINE Target)": {
      "main": [
        [
          {
            "node": "LineMessageNode",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "IF: should_send_line": {
      "main": [
        [
          {
            "node": "Set (LINE Target)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "To InfluxDB Line Protocol": {
      "main": [
        [
          {
            "node": "HTTP Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "availableInMCP": false
  },
  "versionId": "a88bc2e3-2663-4e08-ac79-5ee2531c27fb",
  "meta": {
    "templateCredsSetupCompleted": true,
    "instanceId": "d39bd5346c9c9f532a6f31d698add8ca3598f8f38db76836268bccc2bd6c7e10"
  },
  "nodeGroups": [],
  "id": "21g2hbD2uOm1GIMY",
  "tags": []
}